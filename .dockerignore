# 2026-08-18 更新，待GINO重新審閱
# ⚠️ 這個檔案必須留在專案根目錄，不能跟著 dockerfile 一起搬進 docker/。
#    Docker 找的是「build context 根目錄」底下的 .dockerignore，而 context 是專案根目錄。
#    搬走它不會報錯，只會靜默失效，然後 image 突然大一整個 .cache/。
#
# 三個 image（docker/dockerfile、dockerfile.stt、dockerfile.sourcing）共用這份規則。
# dockerfile 是 COPY . .，沒有這份檔案的話下面這些東西會被打包進去。
# 開發時一律用 -v 掛載專案資料夾，被排除的內容容器仍然看得到，不影響執行。

# 本地筆記與跨模型評測框架：不進 image。
notes/
eval/
_local/
CLAUDE.md

# 執行產出：跑起來才有的東西，不該進 image
data/
baseline/results/

# HF 模型快取：單一模型可達 1.4GB，進 image 等於每次 build 都搬一次
.cache/

# 版控與工具設定
.git/
.gitignore
.claude/
__pycache__/
*.pyc

# 文件：改 README 不該觸發 image 重建
*.md

# 金鑰：走 env_file 帶進容器，不烘進 image 層
.env
.secrets/
