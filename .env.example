# 複製成 .env 後填寫。.env 不進版控。
# compose 設了 required: false，所以沒有 .env 也能 build、也能跑 baseline。

# Hugging Face token。
# 目前用到的模型與資料集都是公開的，不填也能跑；填了才會用
# （下載限流時比較穩，之後若要取用非公開資源則為必填）。
HF_TOKEN=

# SMP2020-EWECT 原始資料的所在位置，只有 eval/scripts/build_testset.py 會用到。
# 那份資料有自身授權條款、不隨本 repo 散布，所以放在專案外層自己管理。
#
# 留空或整個不設定也沒關係：compose 會改掛 docker/empty/（一個空資料夾），
# baseline 與 checks/ 完全不受影響，只有 eval/ 建測試集那一步跑不了。
#
# ⚠️ 建議填絕對路徑。compose 檔放在 docker/ 底下，相對路徑是相對於「那個資料夾」
#    而不是專案根目錄 —— 專案外層的 refs/ 寫成相對路徑會是 ../../refs，很容易寫錯。
#
# ⚠️ 指到 raw 那一層的上層即可，程式會自己接
#    BERT_SMP2020-EWECT/data/raw/usual_test_labeled.txt。
#    不要指向 data/clean/：那份清洗把 [惊恐] 改成《惊恐》，毀掉方括號表情
#    這個情緒訊號。
REFS_DIR=C:/Users/你的帳號/.../imood/refs
