# STT（音檔轉文字）專用依賴。


faster-whisper                  # 後端是 ctranslate2 不是 torch，
                                # 走 CUDA 需要 base image 提供的 cuBLAS 與 cuDNN 9
numpy                           # 暖機用的空音訊緩衝
