# 本地筆記
CLAUDE.md
notes/

# JoyGen 素材清單與交辦內容：來源是別人的 repo，不隨本專案散布
tmp/

# 執行期產物：抽樣樣本、逐句推論結果、下游封包
_local/

# f2（sourcing/ 抓抖音用）會在專案根目錄自己開 logs/ 寫執行紀錄
logs/

# 跨模型評測框架：程式碼（configs/evalkit/scripts）正常進版控，讓協作者看得到評測邏輯；
# 但底下這兩個目錄不進：
#   data/    資料集原文，有自身授權條款；反正由固定 seed 的腳本重建，存檔案沒意義
#   results/ predictions.jsonl 是逐句明細，一樣含資料集原文
# 不含原文的 report.md / metrics.json 若要進版控，個別 git add -f。
eval/data/
eval/results/

# 逐句推論結果含資料集原文；baseline/results/ 只收不含原文的摘要
baseline/results/*.jsonl

# 金鑰（HF_TOKEN 走 env_file 帶進容器，不進版控）
.env
.secrets/

# HF 模型快取（單一模型可達 1.4GB）
.cache/

# Python 執行時產生的快取
__pycache__/
*.pyc

# Claude Code 本機設定：各人機器不同，不共用
.claude/settings.local.json

# 之後訓練會產生的東西
checkpoints/
runs/
*.bin
*.safetensors


research/
