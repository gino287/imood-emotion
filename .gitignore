# 另一套評測框架與其凍結測試集。所用資料集有自身授權條款，不隨本 repo 散布；
# 測試集由固定 seed 的腳本重建，存檔案沒有意義。
_sandbox/

# 本地工作檔。這三條規則涵蓋所有本地檔，新增檔案時不必逐一記得加。
CLAUDE.md
_notes/
*.local.md

# 執行期產物：抽樣樣本、逐句推論結果
_local/

# 本機專屬的 compose 疊加層（掛載路徑因人而異）
docker-compose.override.yml

# 逐句推論結果含資料集原文；results/ 只收不含原文的摘要
results/*.jsonl

# 金鑰（HF_TOKEN 走 --env-file 帶進容器，不進版控）
.env
.secrets/

# HF 模型快取（單一模型可達 1.4GB）
.cache/

# Python 執行時產生的快取
__pycache__/
*.pyc

# Claude Code 本機設定：各人機器不同，不共用
.claude/settings.local.json

# 之後訓練會產生的東西
checkpoints/
runs/
*.bin
*.safetensors
