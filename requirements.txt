# BERT 情緒分類（imood_emotion/ + scripts/ + checks/ + eval/）的依賴。
#
# ⚠️ 不要在這裡指定 torch / torchvision。
#    base image (pytorch/pytorch:2.13.0-cuda12.6-cudnn9-runtime) 已內建一組
#    互相對得上的 CUDA/cuDNN/PyTorch 版本，重裝會造成版本衝突。
#
# ⚠️ 也不要在這裡加 faster-whisper。語音轉文字是可替換的前置模組，
#    依賴列在 stt/requirements.txt，用 docker/dockerfile.stt 另外建 image ——
#    只想要 BERT 情緒分類的人不該被迫連帶抓一整套語音辨識依賴。

transformers==5.14.1            # 釘版本：已驗證可用的版本
datasets                        # 取用 Hugging Face 上的樣本資料集
requests

numpy                           # 統計指標自行以 numpy 實作。
                                # 刻意不裝 scikit-learn：所需公式皆為短函式，
                                # 少一個含 scipy 的依賴，image 小幾百 MB
PyYAML
tqdm                            # 長時間執行需要進度顯示

opencc-python-reimplemented==0.1.7   # 簡→繁 s2twp，前處理用。純 Python 實作免編譯，
                                # Linux 容器與 Windows 本機行為一致。
                                # ⚠️ 釘版本：轉換結果會影響 eval/ 凍結資料集的雜湊值，
                                # 換版本等於換一份測試集，先前的數字就不可比了

nvidia-ml-py                    # 讀 process 真實 VRAM 佔用（含 CUDA context）。
                                # 注意 import 名稱是 pynvml，不是 nvidia_ml_py
